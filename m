# Clone your new repo
git clone https://github.com/wworksmobliedetail-cloud/wworks-mobile-detail.git
cd wworks-mobile-detail

# Add the files (index.html, styles.css, script.js, etc.)
# Copy the files above into this directory

# Stage, commit, and push
git add .
git commit -m "Initial commit: Add website files and assets"
git push origin main
